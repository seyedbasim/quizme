"""WebApp adapter — FastAPI (FR-46..FR-52).

A thin surface over the application layer (AD-11): it holds no business logic the
pipeline and quiz flows don't also enforce. No durable state on the local
filesystem (AD-17).
"""
